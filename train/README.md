About
=====

This is the directory for training the neural networks for encoding or decoding the data.

You'll need to download the COCO image dataset and place the images respectively into `data/train`, `data/val` and `data/test`.

Use VS Code to run the training loop.
